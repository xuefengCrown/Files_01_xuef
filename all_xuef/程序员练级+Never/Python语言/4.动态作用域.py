

##And yes, dynamic scoping is that simple; the value of a variable is determined by
##the closest calling namespace where a value was set. View the calls as a stack,
##and a value is looked up by searching the current call stack from top to bottom.

