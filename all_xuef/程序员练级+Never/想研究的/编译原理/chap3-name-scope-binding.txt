{Name}

#Name

名字使饿哦们可以采用符号形式的标识符引用变量、常量、操作和类型等等，
而不是采用更低级的概念，例如地址。

从抽象的角度看，我们将名字关联于一段可能非常复杂的程序片段，使我们可以将
它堪称是一种作用或者功能，而不必关心这种功能是如何实现的。
通过隐藏起其中的无关细节，抽象能够减少概念的复杂性，使程序员可以在任何特定
时刻把注意力集中到程序正文里的一个可以把握的子集上。
类是数据抽象，使程序员可以将数据表示的细节隐藏到简单的一组操作的后面。

