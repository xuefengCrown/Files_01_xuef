
#define N 10

void main(){

	int arr[N]; // N为常量，可以作为数组长度参数
	// const类型不是常量，不可以作为数组长度参数
}

// 二维数组
int a[2][3] = {0};

// 将二维数组看作一个一维数组，每个元素是一个一维数组
a[2][3] = {{1,2,3}, {4,5,6}};  // 两个一维数组为 a[0], a[1]
// &a[i][j]  === a[i] + j

// n维数组的第一个维度可以省略
int b[][3] = {{1,2,3}};


// 二维数组实例---矩阵转置
// 应用：图片旋转


