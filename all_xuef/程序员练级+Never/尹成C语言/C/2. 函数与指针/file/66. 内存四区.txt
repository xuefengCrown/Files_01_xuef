静态区、代码区、堆区、栈区


void main(){

	int *p = (int *)malloc(10); // p在栈区，p指向的内存在堆区
}

## 生命周期
栈区，堆区都是动态存储区，栈区内存在函数调用的时候分配，函数结束后会被自动回收。
堆区，自己自由分配，自己决定何时回收。
代码区，静态区，就一直存在于程序共存亡。

## 作用域
能够调用的地方的集合

## 可见域 
实际上可访问的区域