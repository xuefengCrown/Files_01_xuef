#include<memory.h>

char str[20] = "hello, xuef.";
memset(str, 'A', 5);


// memcpy 字节拷贝(不管是什么类型，都是拷贝二进制数据)

int a[] = {1,3,5,7,9};
int b[] = {2,4,6,8,10};
memcpy(a, b, 8); // 每个元素占4字节，拷贝8个字节，即2个元素


// memchr 内存中检索字符
_memicmp












